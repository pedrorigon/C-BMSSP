[project]
name = "c-bmssp-benchmarks"
version = "0.1.0"
description = "Benchmark and scalability harness for the C-BMSSP shortest-path solvers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.9",
    "pillow>=10",
    "scipy>=1.13",
]

[tool.uv]
package = false
